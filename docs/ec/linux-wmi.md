# Linux WMI notes moved

The OneXPlayer-specific WMI protocol, GUID, Integer Arg2 packing, binding rules and X2 Mini validation are now maintained in [oxp-wmi.md](oxp-wmi.md).

The old MSI Claw comparison and duplicated kernel-WMI background were removed from the canonical EC documentation. Use the kernel's WMI documentation directly when generic framework details are needed.

This compatibility stub remains so older links do not break.
